# Template Commands

## Ubuntu 24.04 Template

pct create <pct> local:vztmpl/ubuntu-24.04-standard_24.04-2_amd64.tar.zst \
  --hostname <hostname> \
  --cores 1 \
  --memory 1024 \
  --swap 512 \
  --net0 name=eth0,bridge=<vlanid>,ip=dhcp \
  --storage local-lvm \
  --rootfs local-lvm:8 \
  --unprivileged 1 \
  --features nesting=1 \
  --password